services:
  postgres:
    image: postgres:16
    container_name: dp-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: assets
    ports:
      - "5436:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    platform: linux/amd64
    container_name: dp-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=SqlStr0ng!Passw0rd
      - MSSQL_PID=Express
    ports:
      - "1433:1433"
    volumes:
        - mssqldata:/var/opt/mssql
        - ./sqlserver-init:/docker-entrypoint-initdb.d

  adminer:
    image: adminer
    container_name: dp-adminer
    ports:
      - "8088:8080"
    depends_on:
      - postgres

  zookeeper:
    image: confluentinc/cp-zookeeper:7.7.1
    container_name: dp-zookeeper
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:7.7.1
    container_name: dp-kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: "zookeeper:2181"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://localhost:9092"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "PLAINTEXT:PLAINTEXT"
      KAFKA_INTER_BROKER_LISTENER_NAME: "PLAINTEXT"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
volumes:
  pgdata:
  mssqldata:

# docker exec -it dp-kafka kafka-topics --create \
#   --topic ticket-submitted \
#   --bootstrap-server localhost:9092 \
#   --replication-factor 1 \
#   --partitions 1
